Ты эксперт по недвижимости. Используй сведения об объекте ниже и составь короткую памятку для покупателя (читаемый текст, без JSON и без кодовых блоков).

Формат: верни компактный JSON (без markdown и текста вокруг) со схемой:
{
  "summary": "1–2 предложения: тип, локация, метраж, состояние/год, ключевой плюс",
  "recommendation": "следующий шаг",
  "risk_score": 0.0,             // 0..1
  "price_range": { "min_value": 0, "max_value": 0, "currency": "RUB" },
  "pros": ["до 4 сильных стороны"],
  "cons": ["до 4 рисков/минусов"],
  "checks": ["до 4 проверок перед сделкой"]
}
Требования: коротко, без воды; если данных мало — ставь best-effort, но держи валидный JSON.
Если есть поле images (список URL), используй описание/намёки на состояние/ремонт/окружение для оценки рисков и плюсов.

Данные об объекте:
{{PROPERTY_JSON}}
